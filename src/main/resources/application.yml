spring:
  data:
    mongodb:
      uri: mongodb+srv://${mongoUser}:${mongoPass}@fines-database.h45cp.mongodb.net/?retryWrites=true&w=majority&appName=fines-database
      database: fines-database
  security:
    user:
      name: admin
      password: password
server:
  port: 8081